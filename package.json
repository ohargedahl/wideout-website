{
  "name": "wideout-website",
  "private": true,
  "description": "wideout.se — static pages. Tailwind is built to assets/tailwind.css and committed, because GitHub Pages serves this repo as-is with no build step.",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o assets/tailwind.css --minify",
    "watch": "tailwindcss -i src/input.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
